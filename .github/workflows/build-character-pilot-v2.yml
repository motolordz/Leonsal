name: Retired LeonSal character pilot v2 workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "Retired: the pilot chart-crop extraction workflow must not generate or push runtime character assets."
          echo "Current production art policy allows only owner-approved assets to resolve in runtime."
